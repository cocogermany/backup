/**
 * Coco Germany Practice App - Schreiben Player Bridge
 * practice/schreiben-player.js
 *
 * Forwards to the dedicated component implementation in components/schreiben-player.js.
 */

// If components/schreiben-player.js has already run, window.SchreibenPlayerComponent is ready.
if (typeof window !== "undefined" && window.SchreibenPlayerComponent) {
  // Already defined
}
